% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrmm.R
\name{mrmm_fit}
\alias{mrmm_fit}
\title{Fit a multi-resolution mixed model (MRMM) on the sphere}
\usage{
mrmm_fit(
  z,
  loc,
  knots,
  k,
  taper_range,
  matern_range,
  sill,
  nugget,
  smoothness = 0.5
)
}
\arguments{
\item{z}{Numeric vector of observations at the training locations.}

\item{loc}{Two-column numeric matrix of \verb{(latitude, longitude)} in
degrees giving the training locations. Must have \code{length(z)} rows.}

\item{knots}{Two-column numeric matrix of \verb{(latitude, longitude)} in
degrees giving the knot locations of the MRTS basis.}

\item{k}{Integer. Number of MRTS basis functions to use
(\verb{2 <= k <= nrow(knots)}).}

\item{taper_range}{Wendland compact-support range \eqn{a} (radians on
the unit sphere).}

\item{matern_range}{Matérn range parameter \eqn{c}.}

\item{sill}{Sill of the structured residual component
(\eqn{\sigma_y^2}).}

\item{nugget}{Nugget variance (\eqn{\sigma^2_\varepsilon}); diagonal
added to the covariance.}

\item{smoothness}{Matérn smoothness \eqn{\nu}. Default \code{0.5}.}
}
\value{
An object of class \code{"mrmm"}: a list with components
\describe{
\item{\code{coefficients}}{GLS estimate \eqn{\hat\beta}, length \code{k}.}
\item{\code{fitted}, \code{residuals}}{Fitted values and residuals at the
training locations.}
\item{\code{coef_cov}}{The matrix \eqn{(B^\top\Sigma^{-1}B)^{-1}}, used by
\code{\link[=predict.mrmm]{predict.mrmm()}} for standard errors.}
\item{\code{sigma_chol}}{Upper-triangular Cholesky factor of \eqn{\Sigma}
at the training locations.}
\item{\code{basis_train}, \code{train_loc}, \code{train_z}}{Stored training
basis, locations, and observations.}
\item{\code{knots}, \code{k}, \code{params}, \code{call}}{Inputs preserved on the fit.}
}
}
\description{
Combines the \code{\link[=mrts_sphere]{mrts_sphere()}} basis with a tapered Matérn residual
covariance and obtains the basis coefficients by generalised least
squares (GLS).
}
\details{
Given training observations \eqn{z} at locations \eqn{s_1,\ldots,s_n}
on the sphere, the model is
\deqn{z(s) = B(s)\,\beta + \varepsilon(s),\qquad
      \mathrm{Cov}(\varepsilon(s_i),\varepsilon(s_j)) =
      \Sigma_{ij},}{
  z(s) = B(s) beta + epsilon(s),  Cov(epsilon_i, epsilon_j) = Sigma_ij,}
where \eqn{B(s)} is the MRTS basis at location \eqn{s} and \eqn{\Sigma}
is the tapered-Matérn covariance built by
\code{\link[=tapered_matern_sphere]{tapered_matern_sphere()}} using the user-supplied parameters. The
GLS estimate
\deqn{\hat{\beta} = (B^\top \Sigma^{-1} B)^{-1} B^\top \Sigma^{-1} z}{
  beta_hat = (B' Sigma^{-1} B)^{-1} B' Sigma^{-1} z}
is computed via a Cholesky factorisation of \eqn{\Sigma}. Predictions
(mean and standard error) at new locations are obtained with
\code{\link[=predict.mrmm]{predict.mrmm()}}.

Covariance parameters must be supplied. A separate parameter-estimation
routine (\code{mrmm_estimate_params()}) is planned for a future release.
}
\examples{
set.seed(1)
n <- 80
loc <- cbind(runif(n, -60, 60), runif(n, -180, 180))
z   <- 2 + sin(loc[, 1] * pi / 180) + rnorm(n, sd = 0.3)

knots <- loc[sample.int(n, 30), ]
fit <- mrmm_fit(z = z, loc = loc, knots = knots, k = 8,
                taper_range  = 1.0,
                matern_range = 0.2,
                sill         = 0.5,
                nugget       = 0.1,
                smoothness   = 0.5)
fit
length(fit$coefficients)   # k

}
\references{
Huang, H.-Y., Huang, H.-C., and Ing, C.-K. (2025). Multi-Resolution
Spatial Methods on the Sphere: Efficient Prediction for Global Data.
\emph{Environmetrics}. \doi{10.1002/env.70092}
}
\seealso{
\code{\link[=predict.mrmm]{predict.mrmm()}}, \code{\link[=mrts_sphere]{mrts_sphere()}},
\code{\link[=tapered_matern_sphere]{tapered_matern_sphere()}}.
}
