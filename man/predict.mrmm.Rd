% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_mrmm.R
\name{predict.mrmm}
\alias{predict.mrmm}
\title{Predict from a multi-resolution mixed model fit}
\usage{
\method{predict}{mrmm}(object, newdata, se.fit = TRUE, ...)
}
\arguments{
\item{object}{An object of class \code{"mrmm"} from \code{\link[=mrmm_fit]{mrmm_fit()}}.}

\item{newdata}{Two-column numeric matrix of \verb{(latitude, longitude)}
in degrees giving the prediction locations.}

\item{se.fit}{Logical; if \code{TRUE} (default), also return the
prediction standard error at each new location.}

\item{...}{Currently unused.}
}
\value{
A list with
\describe{
\item{\code{fit}}{Numeric vector of predicted values, one per row of
\code{newdata}.}
\item{\code{se.fit}}{Numeric vector of prediction standard errors (only
if \code{se.fit = TRUE}).}
}
}
\description{
Computes kriging-style predictions and (optionally) prediction
standard errors at new spherical locations from an \code{\link[=mrmm_fit]{mrmm_fit()}}
object.
}
\details{
Given the GLS coefficient \eqn{\hat\beta} stored in \code{object}, the
prediction at a new location \eqn{s^*} is
\deqn{\hat z(s^*) = B(s^*)\hat\beta + \Sigma(s^*, S_{\mathrm{train}})\,
                    \Sigma_{\mathrm{train}}^{-1}\,(z - B\hat\beta).}{
  z_hat(s*) = B(s*) beta_hat + Sigma(s*, S_train) Sigma_train^{-1} (z - B beta_hat).}
The prediction variance is the closed-form expression that arises in
kriging with linear-trend basis (sometimes called "universal
kriging"); see the reference.
}
\examples{
set.seed(1)
n <- 80
loc <- cbind(runif(n, -60, 60), runif(n, -180, 180))
z   <- 2 + sin(loc[, 1] * pi / 180) + rnorm(n, sd = 0.3)
knots <- loc[sample.int(n, 30), ]
fit <- mrmm_fit(z = z, loc = loc, knots = knots, k = 8,
                taper_range = 1.0, matern_range = 0.2,
                sill = 0.5, nugget = 0.1)

new_loc <- cbind(seq(-50, 50, length.out = 5),
                 seq(-100, 100, length.out = 5))
p <- predict(fit, newdata = new_loc, se.fit = TRUE)
p$fit
p$se.fit

}
\references{
Huang, H.-Y., Huang, H.-C., and Ing, C.-K. (2025). Multi-Resolution
Spatial Methods on the Sphere: Efficient Prediction for Global Data.
\emph{Environmetrics}. \doi{10.1002/env.70092}
}
\seealso{
\code{\link[=mrmm_fit]{mrmm_fit()}}.
}
