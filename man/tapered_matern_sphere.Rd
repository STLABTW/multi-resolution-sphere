% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tapered_matern.R
\name{tapered_matern_sphere}
\alias{tapered_matern_sphere}
\title{Tapered Matérn covariance on the sphere}
\usage{
tapered_matern_sphere(
  loc1,
  loc2 = NULL,
  taper_range,
  matern_range,
  smoothness = 0.5,
  sill = 1,
  nugget = 0
)
}
\arguments{
\item{loc1}{Two-column numeric matrix of \verb{(latitude, longitude)}
locations in degrees.}

\item{loc2}{Optional second set of locations. If \code{NULL}, the
covariance is computed within \code{loc1} (symmetric output) and the
nugget is added on the diagonal.}

\item{taper_range}{Compact-support range \eqn{a} of the Wendland
taper, in radians on the unit sphere.}

\item{matern_range}{Range parameter \eqn{c} of the Matérn correlation.}

\item{smoothness}{Smoothness \eqn{\nu} of the Matérn correlation.
Default \code{0.5} (exponential).}

\item{sill}{Marginal variance of the structured component. Must be
non-negative.}

\item{nugget}{Nugget variance, added on the diagonal when
\code{loc2 = NULL}. Must be non-negative.}
}
\value{
An \verb{nrow(loc1) x nrow(loc2)} numeric covariance matrix.
}
\description{
Builds a covariance matrix of the form
}
\details{
\deqn{\mathrm{sill} \cdot \rho_{\mathrm{Mat}}(d;\, c, \nu) \cdot
      W(d;\, a) \;+\; \mathrm{nugget} \cdot I}{
  sill * Matern(d; c, nu) * Wendland(d; a) + nugget * I}

where \eqn{d} is the great-circle distance on the unit sphere
(radians), \eqn{\rho_\mathrm{Mat}} is the Matérn correlation with
range \eqn{c} and smoothness \eqn{\nu}, and \eqn{W} is the
Wendland \eqn{C^2} taper of compact support \eqn{a} (degree
\eqn{k = 2}, dimension \eqn{= 2}). The nugget term is added only when
the matrix is symmetric, i.e. when \code{loc2 = NULL}.
}
\examples{
set.seed(1)
loc <- cbind(runif(20, -60, 60), runif(20, -180, 180))
Sigma <- tapered_matern_sphere(loc,
    taper_range = 0.4,
    matern_range = 0.1,
    smoothness = 0.5,
    sill = 1.0,
    nugget = 0.05)
isSymmetric(Sigma)

}
